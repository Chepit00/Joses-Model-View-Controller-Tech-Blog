<div>
    <h2>{{post.title}}</h2>
    <p>{{post.content}}</p>
    <p>Published by {{post.author}} on {{post.createdAt}}</p>

    <h3>Comments</h3>
    {{#each comments}}
    <p>{{this}}</p>
    {{/each}}

</div>